[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
# E501: line too long — enforced via line-length above
# E741: ambiguous variable names — too noisy for scraper code
ignore = ["E741"]

[tool.pytest.ini_options]
testpaths = ["tests"]
